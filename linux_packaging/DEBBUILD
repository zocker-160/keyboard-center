# Maintainer: zocker_160 <zocker1600 at posteo dot net>

pkgname=G910-gui
pkgver=0.1.4
pkgrel=1
pkgdesc="Application by zocker_160 to edit G-keys on G910."
arch=('x86_64')
url="https://github.com/zocker-160/G910-gui"
license=('GPL3')
depends=('python3' 'python3-pip' 'python3-pyqt5' 'python3-uinput' 'python3-ruamel.yaml' 'python3-usb' 'libnotify-bin')
#conflicts=('')
source=("$pkgname-$pkgver::git+https://github.com/zocker-160/G910-gui.git#tag=$pkgver")
sha256sums=('SKIP')

install=g910gui.install

prepare() {
  cd $pkgname-$pkgver
  # nothing to see here
}

build() {
  cd $pkgname-$pkgver
  # still nothing to see here
}

package() {
  cd $pkgname-$pkgver
  
  install -d -m755 src "$pkgdir"/usr/lib/"$pkgname"
  
  cp -r src/assets "$pkgdir"/usr/lib/"$pkgname"
  cp -r src/config "$pkgdir"/usr/lib/"$pkgname"
  cp -r src/devices "$pkgdir"/usr/lib/"$pkgname"
  cp -r src/gui "$pkgdir"/usr/lib/"$pkgname"
  cp -r src/lib "$pkgdir"/usr/lib/"$pkgname"
  
  install -D -m755 linux_packaging/assets/G910-gui.sh "$pkgdir"/usr/bin/G910-gui
  
  install -D -m755 src/main.py -t "$pkgdir"/usr/lib/"$pkgname"
  install -D -m755 src/service.py -t "$pkgdir"/usr/lib/"$pkgname"
  install -D -m644 src/g910-gui.service -t "$pkgdir"/usr/lib/systemd/user
  
  install -D -m644 linux_packaging/assets/g910gui.png -t "$pkgdir"/usr/share/icons/hicolor/512x512/apps
  install -D -m644 linux_packaging/assets/G910-gui.desktop -t "$pkgdir"/usr/share/applications  
}
